# Drone V2 (placeholder)

This is the placeholder for the next version of the drone. It mirrors the `v1/` layout — fill it in once V2 work begins.

## When to start populating this folder

Create V2 work here when one of these is true:

- We're locking V1 (no more design changes) and starting the next-gen design.
- We're spinning a major hardware variant that warrants a new product line.

Until then, all active engineering work goes in `v1/`.

## Expected layout (same as v1/)

```
v2/
├── README.md
├── hardware/
├── firmware/
├── testing/
├── docs/
│   ├── bom.md
│   └── suppliers.md
└── logbooks/
```

## V2 wish list

<!-- Capture things we'd want to fix or add in V2 here. Each item is fair game when V2 work kicks off. -->

- _example_: replace TPS5450 with cheaper buck regulator (see V1 logbook April-2026)
- _example_: smaller form factor PCB
- _example_:
